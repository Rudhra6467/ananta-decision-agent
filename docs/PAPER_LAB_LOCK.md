# Paper laboratory lock — 2026-09-21

Primary objective: $1,000 / $100 paper intelligence laboratory.

Runtime law: implemented → reviewed → merged → deployed/running → verified.

PR #1 is not live. main is DI-LOOP-v0 until merged and the running Hands process is verified.

paper_take=false. keep=false. exec=false. Continuation BENCHED. SHADOW/WATCH/SET16 are not M2.

G1 funnel v2 distinguishes NEVER_LOOKED from LOOKED_NOTHING_VALID.
G2 coverage audit: zero paper-eligible states. Do not invent strategies.
G5 snapshot builder. G6 exit plan will not open. G7 counterfactual does not rewrite thesis.
G8 requires an operator sentence. G1–G7 complete does not create G8.

Ranking is context-conditioned. No global average leaderboard.
